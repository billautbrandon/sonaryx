{
  "name": "sonaryx",
  "version": "1.0.0",
  "description": "Discord bot with cron scheduling",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "check-releases": "node scripts/checkReleases.js",
    "db:setup": "npx prisma generate && npx prisma db push",
    "db:studio": "npx prisma studio",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f sonaryx-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "discord",
    "bot",
    "cron",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "discord.js": "^14.22.1",
    "dotenv": "^16.6.1",
    "node-cron": "^4.2.1",
    "prisma": "^6.14.0",
    "spotify-web-api-node": "^5.0.2"
  }
}
